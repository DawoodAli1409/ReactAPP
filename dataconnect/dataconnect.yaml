specVersion: "v1"
serviceId: "third-task"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "third-task-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
